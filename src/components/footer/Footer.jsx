import React from 'react';
import { Layout, Typography} from 'antd';

export const Footer = () => {
    return (
        <Layout.Footer>
            <Typography.Title level={3} style={{'textAlign': 'center'}} >Test-Test</Typography.Title>
        </Layout.Footer>
    )
}